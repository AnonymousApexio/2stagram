/**
 * Workspace partagé entre le backend et le frontend.
 *
 * Contient les schémas Zod et les types qui en dérivent avec z.infer.
 * Seul workspace compilé du projet : `npm run build:shared`.
 */

export {};
